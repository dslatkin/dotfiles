{{ if or .env.mac .env.linux -}}
#!/bin/bash
set -euo pipefail

if ! command -v fzf > /dev/null
then
    echo "dotfiles: Installing fzf..."
    brew install --quiet fzf
    fzf --version
else
    echo "dotfiles: fzf is already installed"
fi
{{ end -}}
